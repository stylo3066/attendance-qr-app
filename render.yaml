services:
  - type: web
    name: attendance-qr-backend
    env: node
    rootDir: vercel-proxy
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    autoDeploy: true
    envVars:
      - key: HMAC_SECRET
        value: dev_secret
      - key: NODE_ENV
        value: production
